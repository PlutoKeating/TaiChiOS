# TaiChi Shell

The replaceable interface selector and session entry point. The default profile launches dsh-TUI; console, headless, WebView and future interfaces share the same agent/session services.

