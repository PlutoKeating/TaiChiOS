# Integration tests

End-to-end identity, Provider, Harness, interface and plugin workflows.

`control-plane.spec.ts` currently proves the repository-level 0.2 reference path: delegated authority remains bounded in YOLO mode, a scoped credential reaches only the selected Provider adapter, audit remains secret-free, autonomous resources remain attributable, and guarded confirmation survives model-neutral routing across multiple Providers.
