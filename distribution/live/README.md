# Debian Live build

This directory will be a standard `live-build` project. `auto/` contains reproducible command wrappers; `config/` contains package lists, hooks, includes, bootloader material and archive configuration.

